# T01 — Congelar a base e manter o jogo antigo executável

**Data:** 24/09/2026
**Branch:** `codex/nitrokats-reborn-plan-20260924`
**HEAD de entrada:** `82a5d9e165672bcbe9fb57eb437ec6211e17b2a6`
**Base TPS verificada:** `3f49c8d3cb3df0c31b89b02331d28677b19826f5`
**Resultado:** concluído. A edição legada segue como padrão em `src/App.tsx`.

## Mudanças

- Fixada `@playwright/test` em `1.63.0` e preservadas as versões de Vitest e das demais dependências.
- Adicionados `test:unit`, `test:e2e` e `test:smoke`. Vitest exclui `tests/e2e/**`, evitando executar um spec Playwright como suíte unitária; `test` segue cobrindo as suítes existentes com essa mesma fronteira.
- Criada configuração Playwright com base URL `http://127.0.0.1:4173`, um worker, zero retries e servidor Vite estritamente local.
- Criado smoke real do navegador: um canvas, menu e opções legadas, texto de controles, erros de página e de console. O teste anexa uma captura do menu ao resultado Playwright.
- Ignorados `playwright-report/` e `test-results/`.
- `src/App.tsx` permaneceu sem diff: a inspeção confirmou que ele já encaminha diretamente ao jogo legado, portanto nenhuma reescrita seria necessária para manter a edição antiga como padrão.

## RED causal

Com o spec inicial escrito e antes de instalar/configurar Playwright:

```text
npm run test:e2e -- tests/e2e/legacy-smoke.spec.ts
npm error Missing script: "test:e2e"
exit 1
```

Foi um RED da capacidade de execução ausente, não uma regressão do contrato visual. Ao rodar `npm test` nesse estado intermediário, as 5 suítes legadas (8 testes) passaram; o Vitest também tentou coletar o arquivo Playwright e falhou porque `@playwright/test` ainda não estava instalado. A exclusão explícita de `tests/e2e/**` corrigiu essa colisão.

## GREEN e verificações finais

Executados após `npm ci` com o lock atualizado:

| Comando | Resultado observado |
|---|---|
| `npm run test:e2e -- tests/e2e/legacy-smoke.spec.ts` | exit 0; 1 teste passou, Chromium local, canvas único, menu/controles visíveis, sem erros de página/console |
| `npm run test:unit` | exit 0; 5 arquivos, 8 testes passaram |
| `npm run test:smoke` | exit 0; 1 teste passou |
| `npm run lint` | exit 0 |
| `npm run build` | exit 0; 677 módulos transformados |

O smoke salvou `legacy-menu.png` e anexou a imagem ao resultado em `test-results/legacy-smoke-a-base-ainda-abre-um-único-canvas-smoke/`. A pasta é ignorada pelo Git e o artefato não foi promovido a fonte documental canônica.

## Revisão e pendências

A tela legada continua mostrando os controles de jogador único (WASD, botão esquerdo do mouse, mira pelo mouse e ESC para pausar). Este registro não declara controles cooperativos nem hardware de gamepad validados; isso pertence às tarefas posteriores. O build manteve os avisos existentes de Browserslist desatualizado e chunk Three.js acima de 500 kB. A instalação npm reportou 19 vulnerabilidades; nenhuma correção automática ou upgrade amplo foi aplicado.
